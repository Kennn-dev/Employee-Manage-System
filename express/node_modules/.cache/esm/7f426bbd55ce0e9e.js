let gql;_55c‍.x([["default",()=>_55c‍.o]]);_55c‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);
//remmeber import to root file :D
_55c‍.d(gql`
  extend type Query {
    getAllShifts: [Shift]!
    getShiftById(id: ID!): Shift!
  }

  extend type Mutation {
    addShift(newShift: newShiftInput!): Shift!
    deleteShift(id: ID!): Boolean!
    signUpShift(idShift: ID!, idEmployee: ID!): Shift!
  }

  type Shift {
    id: ID!
    name: String!
    employees: [Employee]! #empty array or fill array can be accepted
    timeStart: String!
    timeEnd: String!
    createdAt: String
    updateAt: String
  }

  type status {
    success: Boolean!
  }

  input newShiftInput {
    name: String!
    timeStart: String!
    timeEnd: String!
  }
`);
